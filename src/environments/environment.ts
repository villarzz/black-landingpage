/**
 * Configuração de ambiente de desenvolvimento
 * Este arquivo pode ser substituído durante o build usando `fileReplacements` no angular.json
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  appName: 'Black Landing Page - Crypto Currency Agency (Dev)',
  version: '1.0.0-dev'
};
